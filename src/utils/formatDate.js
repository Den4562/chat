// export function parseFirebaseTime(dateTimeObject) {
//   const millisecind = Math.floor(
//     dateTimeObject.seconds * 1000 + dateTimeObject.nanoseconds / 1000000
//   );
//   const dateTimeObject = new Date(millisecind);
//   const hours = dateTimeObject.getHours();
//   const minutes = dateTimeObject.getMinutes();
//   const timeString = `${hours}:${minutes}`;
//   return timeString;
// }
